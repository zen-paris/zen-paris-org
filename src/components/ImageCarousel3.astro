---
--- 

<div class="carousel3" aria-label="Galerie d’images">
  <div class="carousel3-track">
    <figure class="carousel3-slide active">
      <img src="/assets/josy-thibaut-zen-700x300.webp" alt="Josy Thibaut et la pratique zen" loading="lazy" />
      <figcaption>Josy Thibaut à une manifestation du MLF, le 20 novembre 1971</figcaption>
    </figure>

<figure class="carousel3-slide">
      <img src="/assets/homophobie-zen-700x300.webp" alt="Zen et lutte contre l’homophobie" loading="lazy" />
      <figcaption>Josy Thibaut contre la répression de l’homosexualité le 25 juin 1977</figcaption>
    </figure>

    <figure class="carousel3-slide">
      <img src="/assets/malheurs-sophie-zen-700x300.webp" alt="Les Malheurs de Sophie, lecture zen" loading="lazy" />
      <figcaption> Josy Thibaut parodie Les Malheurs de Sophie. Photo de Catherine Deudon.</figcaption>
    </figure>

    <figure class="carousel3-slide">
      <img src="/assets/manifeste-343-femmes-zen-700x300.webp" alt="Manifeste des 343 femmes et engagement" loading="lazy" />
      <figcaption>Manifeste des 343 paru le 5 avril 1971 dans Le Nouvel Observateur</figcaption>
    </figure>
  </div>

  <button class="carousel3-btn prev" aria-label="Image précédente">‹</button>
  <button class="carousel3-btn next" aria-label="Image suivante">›</button>
</div>

<style>
.carousel3 {
  position: relative;
  width: 100%;
  max-width: 700px;
  margin: 2rem auto;
  overflow: hidden;
}

.carousel3-track {
  display: flex;
  transition: transform 0.4s ease-in-out;
}

.carousel3-slide {
  flex-shrink: 0;
  width: 700px;
  height: 300px;
  text-align: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.carousel3-slide img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* fill nicely without stretching */
  border-radius: 0.5rem;
}

.carousel3-slide figcaption {
  margin-top: 0.5rem;
  font-size: 0.95rem;
  color: #374151;
}

.carousel3-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(0,0,0,0.6);
  color: white;
  border: none;
  font-size: 2rem;
  padding: 0.25rem 0.75rem;
  cursor: pointer;
  z-index: 10;
}
.carousel3-btn.prev { left: 0.5rem; }
.carousel3-btn.next { right: 0.5rem; }

/* RESPONSIVE */
@media (max-width: 720px) {
  .carousel3-slide {
    width: 100%; 
    height: calc(100% * 300 / 700); /* keep aspect ratio 700x300 */
  }
}
</style>

<script type="module" client:load>
const carousel3 = document.querySelector(".carousel3");
const track = carousel3.querySelector(".carousel3-track");
const slides = carousel3.querySelectorAll(".carousel3-slide");
const prev = carousel3.querySelector(".carousel3-btn.prev");
const next = carousel3.querySelector(".carousel3-btn.next");

let index = 0;

function update() {
  const slideWidth = slides[0].getBoundingClientRect().width;
  track.style.transform = `translateX(-${index * slideWidth}px)`;
}

prev.addEventListener("click", () => {
  index = (index - 1 + slides.length) % slides.length;
  update();
});

next.addEventListener("click", () => {
  index = (index + 1) % slides.length;
  update();
});

// Update carousel3 if window resizes
window.addEventListener("resize", update);

// Initial position
update();
</script>

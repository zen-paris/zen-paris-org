---
--- 

<div class="carousel3" aria-label="Galerie d’images">
  <div class="carousel3-track">
    <figure class="carousel3-slide active">
      <img src="/assets/bechet-thibaut-1-700x300.webp" alt="Gilles Thibaut accompagnant Sydney Bechet, l’un des géants du jazz" loading="lazy" />
      <figcaption>Gilles Thibaut accompagnant Sydney Bechet, l’un des géants du jazz</figcaption>
    </figure>

<figure class="carousel3-slide">
      <img src="/assets/entartete-musik-1-700x300.webp" alt="Couverture du catalogue de l’exposition « Musique dégénérée » de 1938 à Düsseldorf" loading="lazy" />
      <figcaption>Couverture du catalogue de l’exposition « Musique dégénérée » de 1938 à Düsseldorf</figcaption>
    </figure>

    <figure class="carousel3-slide">
      <img src="/assets/etoile-swing-700x300.webp" alt="Étoile en carton d’un jeune zazou parisien après l’imposition du port de l’étoile jaune aux juifs" loading="lazy" />
      <figcaption>Étoile en carton d’un jeune zazou parisien après l’imposition du port de l’étoile jaune aux juifs</figcaption>
    </figure>

  <button class="carousel3-btn prev" aria-label="Image précédente">‹</button>
  <button class="carousel3-btn next" aria-label="Image suivante">›</button>
</div>

<style>
.carousel3 {
  position: relative;
  width: 100%;
  max-width: 700px;
  margin: 2rem auto;
  overflow: hidden;
}

.carousel3-track {
  display: flex;
  transition: transform 0.4s ease-in-out;
}

.carousel3-slide {
  flex-shrink: 0;
  width: 700px;
  height: 300px;
  text-align: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.carousel3-slide img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* fill nicely without stretching */
  border-radius: 0.5rem;
}

.carousel3-slide figcaption {
  margin-top: 0.5rem;
  font-size: 0.95rem;
  color: #374151;
}

.carousel3-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(0,0,0,0.6);
  color: white;
  border: none;
  font-size: 2rem;
  padding: 0.25rem 0.75rem;
  cursor: pointer;
  z-index: 10;
}
.carousel3-btn.prev { left: 0.5rem; }
.carousel3-btn.next { right: 0.5rem; }

/* RESPONSIVE */
@media (max-width: 720px) {
  .carousel3-slide {
    width: 100%; 
    height: calc(100% * 300 / 700); /* keep aspect ratio 700x300 */
  }
}
</style>

<script type="module" client:load>
const carousel3 = document.querySelector(".carousel3");
const track = carousel3.querySelector(".carousel3-track");
const slides = carousel3.querySelectorAll(".carousel3-slide");
const prev = carousel3.querySelector(".carousel3-btn.prev");
const next = carousel3.querySelector(".carousel3-btn.next");

let index = 0;

function update() {
  const slideWidth = slides[0].getBoundingClientRect().width;
  track.style.transform = `translateX(-${index * slideWidth}px)`;
}

prev.addEventListener("click", () => {
  index = (index - 1 + slides.length) % slides.length;
  update();
});

next.addEventListener("click", () => {
  index = (index + 1) % slides.length;
  update();
});

// Update carousel3 if window resizes
window.addEventListener("resize", update);

// Initial position
update();
</script>
